// Server base URL used by the app. Change to your server address if needed.
// Use 'http://localhost:3000' when running the Node server locally on Linux.
const String baseUrl = 'http://localhost:3000';
